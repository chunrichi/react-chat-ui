import styled from "styled-components";

const StyledSettingsItem = styled.div``;

const SettingsItemsControl = styled.div`
  display: flex;
  justify-content: space-between;
`;

const StyledSettingsGroup = styled.div``;

const StyledSettings = styled.div`padding: 72px;`;

export default StyledSettings;
export { StyledSettingsItem, SettingsItemsControl, StyledSettingsGroup };
