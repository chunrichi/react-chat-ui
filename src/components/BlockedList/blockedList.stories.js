import React from "react";
import BlockedList from ".";

export default {
  title: "页面组件/BlockedList",
  component: BlockedList,
};

export const Default = () => {
  return <BlockedList>默认</BlockedList>;
};
