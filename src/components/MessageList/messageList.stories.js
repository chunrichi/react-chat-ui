import React from "react";
import MessageList from ".";

export default {
  title: "页面组件/MessageList",
  component: MessageList,
};

export const Default = () => {
  return <MessageList>默认</MessageList>;
};
